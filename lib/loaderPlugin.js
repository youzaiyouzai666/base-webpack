class LoaderPlugin{
    /**
     * 将loader地址添加到 moduleName上
     * @param rules
     * @param moduleName
     */
    async addIdentifiers(rules,moduleName){
        //TODO 处理
        rules.forEach(function(rule){

        });
    }
}

module.exports =new LoaderPlugin();